---
const { class: classExtensions,id } = Astro.props
---

<!-- Class extensions are simple some extra props we are giving to the sections so they can be bigger or smaller depending of the need of the section -->

<section id={id} class={`w-full mx-auto lg:w-[740px] ${classExtensions}`}>
  <slot />
</section>